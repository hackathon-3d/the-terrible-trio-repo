<Page
    x:Class="VisualMove.Gallery"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisualMove"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Button Content="Delete" Name="DeleteButton" Click="DeleteButton_Click" HorizontalAlignment="Stretch" Height="50"/>
            <Button Content="Clear Gallery" Name="ClearButton" Click="ClearButton_Click" HorizontalAlignment="Stretch" Height="50"/>
            <Button Content="Back" Name="BackButton" Click="BackButton_Click" HorizontalAlignment="Stretch" Height="50"/>
            <Button Content="Camera" Name="CameraButton" Click="CameraButton_Click" HorizontalAlignment="Stretch" Height="50"/>
        </StackPanel>
    </Grid>
</Page>